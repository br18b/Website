# Almost-boundary Mandelbrot demo

Precomputed canvas demo for the Fractals II post.

Regenerate with:

```bash
cd scripts/mandelbrot
python3 make_boundary_demo.py --config boundary_demo_windows.json
```
